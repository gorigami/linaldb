% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataset.R
\name{linal_embedded_dataset}
\alias{linal_embedded_dataset}
\alias{linal_embedded_dataset_manifest}
\alias{linal_embedded_dataset_schema}
\alias{linal_embedded_dataset_stats}
\alias{linal_embedded_dataset_read}
\alias{linal_embedded_dataset_to_arrow}
\title{A handle to a saved dataset's on-disk package}
\usage{
linal_embedded_dataset(db, name)

linal_embedded_dataset_manifest(ds)

linal_embedded_dataset_schema(ds)

linal_embedded_dataset_stats(ds)

linal_embedded_dataset_read(ds)

linal_embedded_dataset_to_arrow(ds)
}
\arguments{
\item{db}{A `linal_embedded_db` from `linal_embedded_db()`.}

\item{name}{Dataset name.}

\item{ds}{A `linal_embedded_dataset` from `linal_embedded_dataset()`.}
}
\value{
A `linal_embedded_dataset` object, passed to
  `linal_embedded_dataset_schema()`/`linal_embedded_dataset_manifest()`/
  `linal_embedded_dataset_stats()`/`linal_embedded_dataset_to_arrow()`/
  `linal_embedded_dataset_read()`.
}
\description{
A handle to a saved dataset's on-disk package

Read `data.parquet` directly off disk and return it as a `data.frame`,
transparently unwrapping any column that landed in the legacy
JSON-string fallback encoding back into a real list-column -- the
caller never sees the raw tagged-JSON text. Mirrors
`clients/r/R/dataset.R`'s `linal_dataset_read()`, minus the HTTP fetch.

`arrow::arrow_table(linal_embedded_dataset_read(ds))` -- for callers
who want to keep working in Arrow rather than convert to base R types.
}
